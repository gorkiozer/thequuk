# TheQuuk — local environment
# Mapbox public access token (pk.eyJ...).
# Get one for free at https://account.mapbox.com/auth/signup/
# After adding, restart `npm run dev`.

NEXT_PUBLIC_MAPBOX_TOKEN=
